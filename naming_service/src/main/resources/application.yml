server:
  port: 8761

spring:
  application:
    name: RoombiEureka
  cloud:
    config:
      uri: http://127.0.0.1:8888
      name: config

eureka:
  client:
    register-with-eureka: false
    fetch-registry: false